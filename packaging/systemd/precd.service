[Unit]
Description=prec daemon
After=network.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/usr/sbin/precd --config /etc/prec/precd.conf
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=2
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target
